/// <reference types="react" />
import { SvgProps, NumberProp } from "react-native-svg";
interface Props extends SvgProps {
    size?: NumberProp;
}
declare const BadgeCheckIcon: ({ size, ...props }: Props) => JSX.Element;
export default BadgeCheckIcon;
