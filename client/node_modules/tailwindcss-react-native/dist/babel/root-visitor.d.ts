import { Program } from "@babel/types";
import { NodePath } from "@babel/traverse";
import { TailwindcssReactNativeBabelOptions, State } from "./types";
export default function rootVisitor(options: TailwindcssReactNativeBabelOptions, cwd: string): {
    visitor: {
        Program: {
            enter(path: NodePath<Program>, state: State): void;
        };
    };
};
