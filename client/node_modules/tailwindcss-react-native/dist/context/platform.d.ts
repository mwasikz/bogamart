import { PropsWithChildren } from "react";
export interface PlatformContext {
    preview: boolean;
    platform: string;
}
export declare function PlatformProvider({ preview, platform, ...props }: PropsWithChildren<Partial<PlatformContext>>): JSX.Element;
export declare function usePlatform(): PlatformContext;
