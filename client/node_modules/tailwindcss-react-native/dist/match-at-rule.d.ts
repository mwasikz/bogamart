import { ColorSchemeName } from "react-native";
import { ComponentContext } from "./context/component";
import { DeviceMediaContext } from "./context/device-media";
interface MatchAtRuleOptions {
    rule: string;
    params: string;
    hover: boolean;
    active: boolean;
    focus: boolean;
    platform: string;
    colorScheme: ColorSchemeName;
    componentInteraction: ComponentContext;
    deviceMediaContext: DeviceMediaContext;
}
export declare function matchAtRule({ rule, params, hover, active, focus, componentInteraction, platform, colorScheme, deviceMediaContext: { width, height, orientation }, }: MatchAtRuleOptions): boolean | undefined;
export interface MatchChildAtRuleOptions {
    nthChild: number;
    rule: string;
    params: string;
}
export declare function matchChildAtRule({ nthChild, rule, params, }: MatchChildAtRuleOptions): boolean;
export {};
